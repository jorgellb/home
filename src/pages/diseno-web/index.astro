---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/layout/Header.astro';
import Footer from '../../components/layout/Footer.astro';
import Breadcrumb from '../../components/common/Breadcrumb.astro';
import { pueblos, comarcas, comarcaColors, getPueblosByComarca } from '../../data/pueblos-almeria';

const title = 'Diseño Web en toda la Provincia de Almería | Agencia Local';
const description = 'Páginas web profesionales para cualquier municipio de Almería. Tiendas online, SEO local y hosting gratuito. Presupuesto sin compromiso en 24h.';
const canonical = 'https://platanitorico.com/diseno-web/';
---

<BaseLayout
  title={title}
  description={description}
  canonical={canonical}
  image="/imagenes/equipo-diseno-mantenimiento-web.jpg"
  imageAlt="Diseño web en toda la provincia de Almería - Platanito Rico"
  keywords="diseño web almería provincia, agencia web almería, páginas web pueblos almería, diseño web todas las localidades almería"
  jsonLd={[
    {
      "@context": "https://schema.org",
      "@type": "CollectionPage",
      "name": "Diseño Web en toda la Provincia de Almería",
      "description": description,
      "url": canonical,
      "inLanguage": "es",
      "isPartOf": { "@type": "WebSite", "name": "Platanito Rico", "url": "https://platanitorico.com" },
      "breadcrumb": {
        "@type": "BreadcrumbList",
        "itemListElement": [
          { "@type": "ListItem", "position": 1, "name": "Inicio", "item": "https://platanitorico.com/" },
          { "@type": "ListItem", "position": 2, "name": "Diseño Web por Localidad", "item": canonical }
        ]
      }
    },
    {
      "@context": "https://schema.org",
      "@type": "ProfessionalService",
      "name": "Platanito Rico — Diseño Web en Almería",
      "url": "https://platanitorico.com",
      "telephone": "+34657085019",
      "areaServed": { "@type": "AdministrativeArea", "name": "Almería" },
      "address": {
        "@type": "PostalAddress",
        "streetAddress": "Ctra de Ronda, 82",
        "addressLocality": "Vera",
        "addressRegion": "Almería",
        "postalCode": "04620",
        "addressCountry": "ES"
      }
    }
  ]}
>
  <Header />

  <Breadcrumb items={[
    { label: 'Inicio', href: '/' },
    { label: 'Diseño Web por Localidad' }
  ]} />

  <main id="main-content">

    <!-- Hero -->
    <section class="section-brutal flex items-center justify-center bg-brutal-bg border-b border-brutal-border">
      <div class="container-brutal py-24 text-center">
        <span class="badge-brutal mb-8">
          <svg class="w-4 h-4 text-brutal-accent" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
            <path stroke-linecap="round" stroke-linejoin="round" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
          </svg>
          {pueblos.length} municipios · 7 comarcas · toda Almería
        </span>

        <h1 class="heading-display text-display-sm md:text-display-md lg:text-display-lg text-brutal-text mb-6">
          Diseño Web en<br/>
          <span class="text-brutal-accent">toda la Provincia</span>
        </h1>
        <p class="text-lg text-brutal-text-secondary max-w-2xl mx-auto mb-10 text-body">
          Trabajamos con negocios de cualquier municipio de Almería. Encuentra tu localidad y descubre cómo podemos posicionar tu negocio en Google.
        </p>
        <div class="flex flex-wrap gap-4 justify-center">
          <a href="/contacto/" class="btn-brutal">
            Presupuesto gratis
          </a>
          <a href="/desarrollo-web/" class="btn-brutal-outline">
            Ver servicios
          </a>
        </div>
      </div>
    </section>

    <!-- Por comarca -->
    {comarcas.map(comarca => {
      const pueblosComarca = getPueblosByComarca(comarca);
      const colors = comarcaColors[comarca];
      if (!pueblosComarca.length) return null;
      return (
        <section class="section-brutal py-16 bg-brutal-bg-alt border-b border-brutal-border last:border-0">
          <div class="container-brutal">
            <div class="flex items-center gap-4 mb-8">
              <span class={`badge-brutal ${colors.badge}`}>
                <svg class={`w-4 h-4 ${colors.icon}`} fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7" />
                </svg>
                {comarca}
              </span>
              <span class="text-sm text-brutal-text-muted">{pueblosComarca.length} municipios</span>
            </div>

            <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 gap-3">
              {pueblosComarca.map(p => (
                <a
                  href={`/diseno-web/${p.slug}/`}
                  class={`card-brutal group flex flex-col p-4 bg-brutal-card border-brutal-border ${colors.shadow}`}
                >
                  {p.destacado && (
                    <span class="absolute top-2 right-2 w-2 h-2 bg-brutal-accent"></span>
                  )}
                  <span class="text-sm font-semibold text-brutal-text group-hover:text-brutal-accent leading-tight mb-1">
                    {p.nombre}
                  </span>
                  {p.sectorPrincipal ? (
                    <span class="text-xs text-brutal-text-muted line-clamp-2 leading-tight">{p.sectorPrincipal}</span>
                  ) : p.poblacion ? (
                    <span class="text-xs text-brutal-text-muted">{p.poblacion.toLocaleString('es-ES')} hab.</span>
                  ) : null}
                </a>
              ))}
            </div>
          </div>
        </section>
      );
    })}

    <!-- CTA final -->
    <section class="section-brutal py-24 bg-brutal-accent">
      <div class="container-brutal text-center">
        <h2 class="heading-display text-display-sm md:text-display-md text-brutal-text mb-4">
          ¿No encuentras tu municipio?
        </h2>
        <p class="text-brutal-text/80 mb-8 max-w-2xl mx-auto text-body">
          Trabajamos con cualquier localidad de la provincia de Almería. Contáctanos y te hacemos un presupuesto personalizado para tu zona.
        </p>
        <a href="/contacto/" class="btn-brutal bg-brutal-text text-brutal-bg hover:bg-brutal-text/90">
          Consulta gratuita sin compromiso
        </a>
      </div>
    </section>

  </main>

  <Footer />
</BaseLayout>
