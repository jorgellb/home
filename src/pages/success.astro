---
import Layout from '../layouts/BaseLayout.astro';
---

<Layout 
  title="Mensaje Recibido" 
  description="Tu mensaje ha sido recibido correctamente. Nuestro equipo te responderá en menos de 24 horas."
  noindex={true}
>
  <main class="relative min-h-screen flex items-center justify-center bg-zinc-950 overflow-hidden">
    <!-- Background Elements -->
    <div class="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.02)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.02)_1px,transparent_1px)] bg-[size:64px_64px]"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-purple-500/10 rounded-full blur-[120px]"></div>

    <div class="relative z-10 text-center px-6 max-w-2xl mx-auto animate-fade-up">
      <!-- Success Icon -->
      <div class="w-24 h-24 mx-auto mb-8 rounded-full bg-gradient-to-br from-emerald-400 to-green-600 p-0.5 shadow-2xl shadow-emerald-500/20">
        <div class="w-full h-full rounded-full bg-zinc-900 flex items-center justify-center">
          <svg class="w-12 h-12 text-emerald-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
          </svg>
        </div>
      </div>

      <h1 class="text-4xl md:text-5xl font-bold text-white mb-6">
        ¡Mensaje <span class="text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 to-green-500">Recibido!</span>
      </h1>
      
      <p class="text-xl text-zinc-400 mb-10 leading-relaxed">
        Gracias por contactar con nosotros. Hemos recibido tu mensaje correctamente y nuestro equipo te responderá en menos de 24 horas.
      </p>

      <a href="/" class="btn-premium inline-flex items-center gap-2 group">
        <svg class="w-5 h-5 group-hover:-translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
        </svg>
        Volver al inicio
      </a>
    </div>
  </main>
</Layout>
