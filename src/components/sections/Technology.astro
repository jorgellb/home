---
// Technology Section - Magical Grid Design
const technologies = {
  Frontend: ['Next.js', 'React', 'TypeScript', 'Tailwind CSS', 'Astro', 'Vite', 'SvelteKit'],
  Diseño: ['Figma', 'Framer', 'Rive', 'Spline', 'Penpot'],
  Audiovisual: ['Runway', 'DaVinci Resolve', 'Unreal Engine 5', 'Adobe Firefly', 'CapCut'],
  Marketing: ['Claude / GPT-4o', 'HubSpot', 'Make', 'Segment', 'Google Analytics 4'],
  Backend: ['Node.js (Bun/Deno)', 'Go', 'Rust', 'PostgreSQL', 'Supabase', 'GraphQL'],
  DevOps: ['Docker', 'Kubernetes', 'Terraform', 'GitHub Actions', 'Vercel', 'Prometheus']
};

const categoryColors: Record<string, string> = {
  Frontend: 'from-orange-500 to-amber-500',
  Diseño: 'from-purple-500 to-pink-500',
  Audiovisual: 'from-red-500 to-orange-500',
  Marketing: 'from-blue-500 to-cyan-500',
  Backend: 'from-green-500 to-emerald-500',
  DevOps: 'from-teal-500 to-cyan-400'
};

const categoryIcons: Record<string, string> = {
  Frontend: '⚡',
  Diseño: '🎨',
  Audiovisual: '🎬',
  Marketing: '📈',
  Backend: '⚙️',
  DevOps: '🚀'
};
---

<section class="relative py-24 overflow-hidden" id="tecnologia">
  <!-- Animated Background -->
  <div class="absolute inset-0 bg-gradient-to-b from-white via-orange-50/30 to-white"></div>
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,rgba(255,107,0,0.03)_0%,transparent_50%)]"></div>
  
  <!-- Floating Particles -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    {[...Array(20)].map((_, i) => (
      <div 
        class="absolute w-2 h-2 rounded-full bg-orange-400/20 animate-float"
        style={{
          left: `${Math.random() * 100}%`,
          top: `${Math.random() * 100}%`,
          animationDelay: `${Math.random() * 5}s`,
          animationDuration: `${3 + Math.random() * 4}s`
        }}
      />
    ))}
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-gradient-to-r from-orange-500/10 to-amber-500/10 border border-orange-500/20 mb-6">
        <span class="text-2xl">✨</span>
        <span class="text-orange-700 font-semibold text-sm uppercase tracking-wider">Stack Moderno</span>
      </div>
      
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold text-slate-900 mb-6">
        Tecnologías que
        <span class="relative inline-block">
          <span class="relative z-10 bg-gradient-to-r from-orange-500 via-amber-500 to-orange-600 bg-clip-text text-transparent">
            dominamos
          </span>
          <svg class="absolute -bottom-2 left-0 w-full h-3 text-orange-300/50" viewBox="0 0 200 12" preserveAspectRatio="none">
            <path d="M0,8 Q50,0 100,8 T200,8" stroke="currentColor" stroke-width="4" fill="none" stroke-linecap="round"/>
          </svg>
        </span>
      </h2>
      
      <p class="text-lg md:text-xl text-slate-600 max-w-3xl mx-auto leading-relaxed">
        Utilizamos las herramientas más avanzadas del mercado para crear experiencias digitales excepcionales
      </p>
    </div>

    <!-- Categories Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6">
      {Object.entries(technologies).map(([category, tools], catIndex) => (
        <div 
          class="group relative"
          style={`animation-delay: ${catIndex * 0.1}s`}
        >
          <!-- Card Container -->
          <div class="relative h-full rounded-3xl bg-white border border-slate-200 p-6 overflow-hidden transition-all duration-500 hover:shadow-2xl hover:shadow-orange-500/10 hover:border-orange-300 group-hover:-translate-y-1">
            
            <!-- Background Gradient on Hover -->
            <div class={`absolute inset-0 bg-gradient-to-br ${categoryColors[category]} opacity-0 group-hover:opacity-[0.03] transition-opacity duration-500`}></div>
            
            <!-- Animated Border Gradient -->
            <div class="absolute inset-0 rounded-3xl opacity-0 group-hover:opacity-100 transition-opacity duration-500 pointer-events-none">
              <div class={`absolute inset-0 rounded-3xl bg-gradient-to-r ${categoryColors[category]} opacity-20 blur-sm`}></div>
            </div>

            <!-- Category Header -->
            <div class="relative flex items-center gap-3 mb-5">
              <div class={`w-12 h-12 rounded-2xl bg-gradient-to-br ${categoryColors[category]} p-[2px] shadow-lg shadow-orange-500/20 group-hover:shadow-orange-500/40 transition-shadow duration-300`}>
                <div class="w-full h-full rounded-2xl bg-white flex items-center justify-center text-2xl">
                  {categoryIcons[category]}
                </div>
              </div>
              <div>
                <h3 class="text-xl font-bold text-slate-900">{category}</h3>
                <div class="flex items-center gap-1 text-sm text-slate-500">
                  <span class="w-2 h-2 rounded-full bg-gradient-to-r from-orange-400 to-amber-400 animate-pulse"></span>
                  <span>{tools.length} tecnologías</span>
                </div>
              </div>
            </div>

            <!-- Tools Grid -->
            <div class="relative grid grid-cols-2 gap-2">
              {tools.map((tool, toolIndex) => (
                <div 
                  class="group/tool relative flex items-center gap-2 px-3 py-2.5 rounded-xl bg-slate-50 border border-slate-100 hover:bg-white hover:border-orange-200 transition-all duration-300 cursor-default overflow-hidden"
                  style={`animation-delay: ${catIndex * 0.1 + toolIndex * 0.05}s`}
                >
                  {/* Shine Effect */}
                  <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/60 to-transparent -translate-x-full group-hover/tool:translate-x-full transition-transform duration-700"></div>
                  
                  {/* Dot Indicator */}
                  <div class={`w-1.5 h-1.5 rounded-full bg-gradient-to-r ${categoryColors[category]} group-hover/tool:scale-150 transition-transform duration-300`}></div>
                  
                  {/* Tool Name */}
                  <span class="text-sm font-medium text-slate-700 group-hover/tool:text-slate-900 transition-colors relative z-10 truncate">
                    {tool}
                  </span>
                </div>
              ))}
            </div>

            {/* Bottom Decoration */}
            <div class="absolute bottom-0 left-0 right-0 h-1 bg-gradient-to-r from-transparent via-orange-300/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 transform scale-x-0 group-hover:scale-x-100"></div>
          </div>
        </div>
      ))}
    </div>

    {/* Bottom Stats Row */}
    <div class="mt-16 grid grid-cols-2 md:grid-cols-4 gap-4">
      {[
        { value: '35+', label: 'Tecnologías', color: 'from-orange-400 to-amber-400' },
        { value: '99/100', label: 'Lighthouse Score', color: 'from-green-400 to-emerald-400' },
        { value: '100%', label: 'Código Limpio', color: 'from-blue-400 to-cyan-400' },
        { value: '24/7', label: 'Soporte', color: 'from-purple-400 to-pink-400' }
      ].map((stat, index) => (
        <div 
          class="group relative text-center p-6 rounded-2xl bg-white border border-slate-200 hover:border-orange-300 transition-all duration-300 hover:shadow-lg"
          style={`animation-delay: ${0.8 + index * 0.1}s`}
        >
          <div class={`text-3xl md:text-4xl font-bold bg-gradient-to-r ${stat.color} bg-clip-text text-transparent mb-2 group-hover:scale-110 transition-transform duration-300`}>
            {stat.value}
          </div>
          <div class="text-sm text-slate-500 font-medium">{stat.label}</div>
          <div class="absolute inset-0 rounded-2xl bg-gradient-to-r from-orange-500/5 to-amber-500/5 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
        </div>
      ))}
    </div>

    {/* CTA Section */}
    <div class="mt-16 text-center">
      <p class="text-slate-600 mb-6 max-w-2xl mx-auto">
        ¿Tu proyecto requiere tecnologías específicas? Trabajamos con el stack que mejor se adapte a tus necesidades.
      </p>
      <a 
        href="/contacto"
        class="group inline-flex items-center gap-2 px-8 py-4 bg-gradient-to-r from-orange-500 to-amber-500 text-white font-semibold rounded-full shadow-lg shadow-orange-500/25 hover:shadow-xl hover:shadow-orange-500/40 transition-all duration-300 hover:-translate-y-0.5"
      >
        <span>Hablemos de tu proyecto</span>
        <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  @keyframes float {
    0%, 100% {
      transform: translateY(0) translateX(0);
      opacity: 0.3;
    }
    25% {
      transform: translateY(-20px) translateX(10px);
      opacity: 0.6;
    }
    50% {
      transform: translateY(-10px) translateX(-5px);
      opacity: 0.4;
    }
    75% {
      transform: translateY(-30px) translateX(5px);
      opacity: 0.5;
    }
  }

  .animate-float {
    animation: float linear infinite;
  }

  /* Stagger animation for cards */
  .group {
    animation: fadeInUp 0.6s ease-out forwards;
    opacity: 0;
    transform: translateY(20px);
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Tool items animation */
  .group\/tool {
    animation: fadeIn 0.4s ease-out forwards;
    opacity: 0;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }
</style>
